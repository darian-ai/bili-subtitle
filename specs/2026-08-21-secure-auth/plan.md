# 阶段三：安全登录实施计划

## 执行状态

- 任务组 1—7：已完成实现与离线自动化验证。
- 任务组 8：本地自动化、依赖与范围审计、真实 Windows Credential Manager 专用测试槽位、Windows GitHub Actions 及全部 Windows 人工流程验收已完成。
- 阶段三全部任务组和合并清单均已完成。

## 1. 建立认证模块边界与依赖

- [x] 将 `keyring` 和 `qrcode` 加入运行时依赖并更新锁文件，不提前加入字幕或导出依赖。
- [x] 创建认证应用编排、认证领域结果、Bilibili 认证适配器和 Credential Manager 适配器模块。
- [x] 定义认证客户端、凭据仓库、时钟/等待和终端二维码渲染端口，使核心流程可离线测试。
- [x] 保持 CLI → 应用层 → 领域/基础设施层的单向依赖，领域层不得导入框架、HTTP 或 keyring。

## 2. 实现会话凭据模型与 Credential Manager 适配器

- [x] 定义带显式版本、Cookie 名值映射和秘密安全表示的不可变会话凭据。
- [x] 实现确定性序列化和严格反序列化，拒绝空、未知版本、非法 JSON 和错误字段类型。
- [x] 使用固定服务名 `bili-subtitle` 和单一默认账号槽位实现读取、保存与幂等清除。
- [x] 将未找到与 keyring 后端的读取、保存、删除失败映射为稳定结果或错误。
- [x] 使用内存替身覆盖全部存储路径，并证明测试不接触真实 Credential Manager。

## 3. 实现 Bilibili 认证 HTTP 适配器

- [x] 集中实现登录状态检查、二维码申请与二维码状态轮询请求，沿用同步 HTTPX Client 和统一超时策略。
- [x] 严格校验 HTTP 状态、平台结果码和响应字段，并映射未扫码、待确认、成功、过期、取消和失败。
- [x] 隔离二维码渲染内容与轮询密钥，使其只存在于单次基础设施登录会话中。
- [x] 登录成功时从受信任 Cookie 容器提取并校验最小会话凭据，缺失必要 Cookie 时拒绝成功。
- [x] 使用脱敏固定响应覆盖正常状态、未知状态、结构异常、HTTP 错误、超时与连接失败。

## 4. 实现终端二维码渲染与有界登录状态机

- [x] 使用 `qrcode` 生成终端矩阵并通过受控渲染接口显示，不输出原始二维码内容或写图片文件。
- [x] 实现申请一次二维码、固定间隔轮询、状态变化提示、总超时和有限瞬时错误重试。
- [x] 对成功、过期、用户取消、超时、平台失败、网络失败和未知状态返回稳定终止结果。
- [x] 登录成功后先保存凭据，只有保存完成才报告成功；任何失败都不写入半有效值。
- [x] 通过注入时钟与等待函数测试完整状态序列，测试不得真实等待或访问终端设备。

## 5. 接通认证子命令

- [x] 用真实认证应用流程替换 `auth login|status|clear` 的阶段一占位行为。
- [x] `auth login` 复用在线验证有效的凭据，否则只启动一次二维码流程并映射退出码。
- [x] `auth status` 区分未登录、凭据无效、已失效、有效和操作错误，且绝不触发登录。
- [x] `auth clear` 仅操作 Credential Manager，凭据不存在时幂等成功且不访问网络。
- [x] 固定各命令的简体中文输出、标准输出/错误边界和 `0|1|2` 退出码。

## 6. 将自动认证接入主命令

- [x] 在应用层为阶段二元数据流程增加认证门控，不让 CLI 直接操作 Cookie、keyring 或认证接口。
- [x] 有效凭据复用同一 HTTP 会话继续原输入和分集选择任务，不申请二维码。
- [x] 凭据缺失、损坏或失效时完成一次扫码登录，保存后自动恢复原任务一次。
- [x] 登录后再次认证失败时立即返回整体失败，防止重复登录或重复应用步骤。
- [x] 验证主命令仍只输出元数据摘要、不创建输出文件，`--lang` 和 `--force` 仍无阶段外副作用。

## 7. 完成安全、架构与回归测试

- [x] 参数化覆盖未扫码、待确认、成功、过期、取消、失败、未知状态和明确超时。
- [x] 覆盖凭据有效复用、无凭据、损坏、失效、保存失败、清除失败及 keyring 后端不可用。
- [x] 覆盖登录后恢复一次、恢复参数不变、再次失效不循环以及元数据错误保持原分类。
- [x] 用伪 Cookie、二维码密钥和二维码内容审计输出、异常、对象表示与测试快照，确保零泄漏。
- [x] 检查默认测试不联网、不读取真实凭据、不真实等待，也不创建字幕或二维码文件。
- [x] 回归阶段一 CLI 和阶段二输入、短链、元数据及分集选择全部行为。

## 8. 完成验证与合并验收

- [x] 执行 [`validation.md`](./validation.md) 的全部自动化命令、认证命令和主流程场景。
- [x] 在 Windows 上通过测试槽位验证 Credential Manager 读写清除，并在结束后清除测试凭据。
- [x] 人工扫码验证二维码显示、成功保存、状态复用、自动恢复、过期/取消终止和清除流程。
- [x] 审计直接与传递依赖，确认没有浏览器自动化、媒体下载、ASR、OCR 或绕过能力。
- [x] 确认 feature spec、实现、测试和锁文件一致，工作区没有无关改动。
- [x] 提交并推送 feature 分支，确认 Windows GitHub Actions 全部质量检查通过（[Quality run 32398440533](https://github.com/darian-ai/bili-subtitle/actions/runs/32398440533)，提交 `234c3ed`）。
