# 阶段五：完整 V1 流程实施计划

## 1. 用失败测试冻结公开流程与聚合模型

- [x] 定义分集、轨道、逐文件动作、摘要和最终退出状态的不可变领域/应用结果。
- [x] 锁定默认全部分集、显式单分集、全部轨道及重复 `--lang` 的选择契约。
- [x] 用架构测试锁定 CLI、应用、领域、基础设施依赖方向及同步串行执行。
- [x] 回归阶段二选择、阶段三一次认证恢复和阶段四忠实导出语义。

## 2. 测试先行实现 Windows 输出路径规划

- [x] 实现非法字符、控制字符、尾随空格/句点、空组件和保留名的确定性净化。
- [x] 用 P 序号、语言和轨道 ID 构造稳定基名，覆盖同语言多轨道及大小写不敏感冲突。
- [x] 为冲突与超长标题加入稳定摘要后缀，并在 240 字符绝对路径预算内保留身份和扩展名。
- [x] 在写入或正文下载前规划整批路径，拒绝越界、无法唯一化或无法满足预算的轨道路径。

## 3. 测试先行扩展逐文件发布策略

- [x] 将阶段四导出端口扩展为 JSON/SRT 分别检查和记录 `written|replaced|skipped`。
- [x] 覆盖两文件均存在、仅缺 JSON、仅缺 SRT、两者均缺及 `--force` 全量替换。
- [x] 证明默认补齐不改变既有字节，`--force` 失败保留旧目标，所有故障清理同目录临时文件。
- [x] 扩展投稿级 manifest，最后由本次完整结果确定性构建并原子替换。

## 4. 测试先行实现单分集全部轨道编排

- [x] 发现一个分集的全部轨道，按平台顺序应用精确语言过滤并保留同语言多轨道。
- [x] 无字幕与过滤无匹配返回不同正常结果，均不下载正文。
- [x] 对每条匹配轨道按文件计划即时下载、校验、补齐或覆盖，并记录逐文件动作。
- [x] 隔离轨道下载、结构、路径与导出失败，证明后续轨道继续且已有成功文件保留。

## 5. 测试先行实现多分集完整流程

- [x] 按阶段二选定顺序串行处理默认全部、URL `p`、`--page` 与 `--all-pages` 分集。
- [x] 隔离分集发现失败与轨道失败；一个项目失败不阻止其他分集/轨道。
- [x] 复用同一 HTTPX Client、有效会话和应用上下文，不并发、不跨投稿、不重复认证。
- [x] 生成覆盖全部本次结果的投稿级 manifest，并保持阶段四身份、安全和原子发布含义。

## 6. 接入安全认证与公开 CLI

- [x] 用完整提取流程替换主命令的阶段三元数据摘要行为，保持现有参数帮助与互斥规则。
- [x] 有效凭据直接复用；失效或缺失时二维码成功后恢复原流程一次，终态失败映射为整体失败。
- [x] 将结构化逐项结果和计数摘要渲染为终端安全的简体中文输出。
- [x] 参数、命令级错误与聚合结果严格映射到退出码 `0|1|2`，不输出 traceback 或秘密。

## 7. 完成离线流程与故障矩阵

- [x] 覆盖单/多分集、全部轨道、多语言、重复过滤值、同语言多轨道和稳定无覆盖输出。
- [x] 覆盖逐文件跳过、两种缺失补齐、`--force`、全跳过、无字幕和全无匹配。
- [x] 参数化分集发现、轨道下载、平台结构、网络、路径、原子发布故障及 manifest 失败。
- [x] 直接证明正常、部分成功、整体失败分别为 `0`、`1`、`2`，且摘要计数与 manifest 一致。

## 8. 完成安全、回归与合并验收

- [x] 执行 [`validation.md`](./validation.md) 的全部本地质量命令、流程矩阵和 Windows 文件系统离线检查。
- [x] 审计伪 Cookie、二维码密钥、签名 URL、响应正文及绝对路径，确认不进入输出、异常、文件或提交。
- [x] 审计依赖与源码，确认无并发、媒体下载、ASR、OCR、翻译、私有接口或访问控制绕过。
- [ ] 确认阶段一至四回归、覆盖率、Ruff、格式、strict Pyright 和待合并提交 Windows CI 全部通过。
- [x] 检查规格、实现、测试、锁文件和工作区一致；不提前记录阶段六真实平台人工矩阵或交付结果。
